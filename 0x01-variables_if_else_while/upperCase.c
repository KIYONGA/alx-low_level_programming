#include <stdio.h>
/**
 * main - entry point
 *
 * Return: always 0 when success
 */
int main(void)
{
	int upperCase = 'A';

		while (upperCase <= 'Z');
		/**
		 * while conditional statement
		 */

	{
		putchar(upperCase);
		upperCase += 1
	}
		putchar('\n');
		return (0);
}
